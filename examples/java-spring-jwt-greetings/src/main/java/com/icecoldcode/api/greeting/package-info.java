/**
 * A small example of a greeting app
 * to show the authentication in action.
 * <p>
 * - anyone can list greetings
 * - you (authenticated) can post a greeting
 * - you (authenticated) can remove your own greeting
 * - an admin can remove any greeting in the same company
 */
package com.icecoldcode.api.greeting;